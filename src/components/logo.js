import PropTypes from 'prop-types';
import { styled } from '@mui/material/styles';
import { height, width } from '@mui/system';

export const Logo = styled((props) => {
  return (
    <img src='static\images\catolica\logo.png' 
      style={ {width: 245, height: 64} }>  
    </img>
  );  
})``;
